<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://fonts.googleapis.com/css?family=Fira+Sans&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="styles/style.css">
        <title>Document</title>
    </head>

    <body>
        <div class="add-new-list">
            <form action="/newlist" method="POST">
                <input class="input-list" type="text" name="title" placeholder="New list name" autocomplete="off">
                <button class="btn-list btn-anim" type="submit">+</button>
            </form>
        </div>
        <div class="main-container">
            <% lists.forEach(element => { %>
            <div class="list" id=<%= element.ID %>>
                <div class="list-title <%= element.color %>">
                    <h4><%= element.title %></h4>
                </div>
                <div class="list-body">
                    <% element.items.forEach(item => { %>

                    <div class="list-item" id=<%= item.ID %>>
                        <input type="checkbox" name="" id="">
                        <p class="item-text"><%= item.value %></p>
                    </div>
                    <% }) %>
                </div>
                <div class="add-item">
                    <form action="/additem" method="POST">
                        <input class="input-item" type="text" name="newItem" placeholder="Add item" autocomplete="off">
                        <button class="btn-item <%= element.color %>" type="submit" name="listID"
                            value=<%= element.ID %>>+</button>
                    </form>
                </div>
            </div>
            <% }) %>
        </div>
    </body>

    <footer></footer>

</html>